# Node Modules Cleaner 项目规则

- 运行时保持 Python 标准库实现，不引入第三方依赖。
- 删除逻辑必须默认 dry-run，且在真正删除前重做路径边界和符号链接检查。
- 任何改动都必须覆盖：旧目录可清理、最近活动不清理、keep marker 不清理、符号链接不跟随、边界根目录拒绝扫描。
- Jenkins 清理阶段前必须保留自动化测试门禁，并归档 JSON 审计报告。
