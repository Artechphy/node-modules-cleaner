# EVD-0001 — 本地验证证据

- 验证时间：2026-09-16（Asia/Shanghai）
- 输入版本：IN-0001/v1
- 验证器：Python 3.9 `unittest` + `py_compile` + CLI 参数回放
- 被测代码：当前工作区

## 已执行命令与结果

1. `python3 -m unittest discover -s tests -v`
   - 退出状态：0
   - 结果：8/8 PASS。
   - 覆盖：audit 不删除、apply 删除旧目录、最近依赖活动、最近项目活动、keep marker、排除规则、链接与嵌套发现、危险根目录拒绝。
2. `PYTHONPYCACHEPREFIX=/tmp/node-modules-cleaner-pycache python3 -m py_compile src/node_modules_cleaner.py`
   - 退出状态：0
   - 结果：Python 3.9 语法编译通过。
3. `./bin/cleanup-node-modules --help`
   - 退出状态：0
   - 结果：`root/days/mode/exclude/exclude-file/report-dir` 参数全部可用。

## 失败与修正记录

- 首轮：4 个用例因 Python 3.9 不支持 `Path.stat(follow_symlinks=...)` 失败；1 个用例因 macOS 临时目录路径别名比较失败。
- 修正：使用 `Path.lstat()`，路径断言使用 `resolve()`。
- 回归：8/8 PASS，原失败集合清零。
